package invitados;
import java.util.HashSet;
public class ListaInvitados{
   private HashSet<String> invitados;
   public ListaInvitados(){
       this.invitados= new HashSet<String>();
   }
   public HashSet<String> invitadosConA{
        HashSet<String> nombres= new HashSet<String>();
        for (String elemento : invitados ){
            if(charAt(int 0)== "A"){
                nombres.add(elemento);
            }
        }
        return nombres;
    }
}